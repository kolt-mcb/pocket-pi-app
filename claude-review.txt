Claude Code deep review of current git diff:

1. CRITICAL — key = { frame.lines[idx] } produces duplicate keys and will throw at runtime.
Blank terminal buffer rows are common and applyMirrorDiff pads buffers with empty strings. Duplicate keys will throw IllegalArgumentException in LazyColumn/Row.

2. Stated rationale for content keys is incorrect.
Lazy list keys control item identity, not recomposition skipping. Per-row memoization is already handled by remember(raw).

3. Content keys regress scroll anchoring.
LazyListState anchors viewport to key. With duplicate empty-string keys, findIndexByKey may resolve an unrelated buffer position on frames containing blank rows.

4. frameHash computation is redundant.
remember(key1) already compares keys with equals; List<String>.equals is deep element-wise. The hash adds a second O(N) pass after the comparison that already gates it.

5. Hash collision silently stops auto-scroll.
Collision means LaunchedEffect does not re-run. Scroll stays pinned to stale bottom until next content change.

6. Performance regression: O(1) key replaced by two O(N) passes per frame.
Old key was single Int compare. New behavior adds deep list-equality scan plus full hash fold per frame on main thread.

7. isScrollInProgress bail has no retry path.
Previously keyed on frame.seq, so scroll bail retried next frame. Now keyed on content, so a frame arriving mid-fling is dropped until next content change.

8. Comment placement and duplication.
New block sits under existing per-line memoization comment and describes a different mechanism.